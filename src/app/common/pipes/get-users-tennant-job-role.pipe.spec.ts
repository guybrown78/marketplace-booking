import { GetUsersTennantJobRolePipe } from './get-users-tennant-job-role.pipe';

describe('GetUsersTennantJobRolePipe', () => {
  it('create an instance', () => {
    const pipe = new GetUsersTennantJobRolePipe();
    expect(pipe).toBeTruthy();
  });
});
