import { GetDisplayPricePipe } from './get-display-price.pipe';

describe('GetDisplayPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDisplayPricePipe();
    expect(pipe).toBeTruthy();
  });
});
