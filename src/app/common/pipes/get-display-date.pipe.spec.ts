import { GetDisplayDatePipe } from './get-display-date.pipe';

describe('GetDisplayDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDisplayDatePipe();
    expect(pipe).toBeTruthy();
  });
});
