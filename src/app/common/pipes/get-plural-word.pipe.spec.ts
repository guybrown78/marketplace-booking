import { GetPluralWordPipe } from './get-plural-word.pipe';

describe('GetPluralWordPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPluralWordPipe();
    expect(pipe).toBeTruthy();
  });
});
