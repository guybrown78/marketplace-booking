import { GetCourseDurationPipe } from './get-course-duration.pipe';

describe('GetCourseDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCourseDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
