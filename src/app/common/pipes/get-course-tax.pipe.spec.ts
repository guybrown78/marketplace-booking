import { GetCourseTaxPipe } from './get-course-tax.pipe';

describe('GetCourseTaxPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCourseTaxPipe();
    expect(pipe).toBeTruthy();
  });
});
