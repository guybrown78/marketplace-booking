import { GetCoursePricePipe } from './get-course-price.pipe';

describe('GetCoursePricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCoursePricePipe();
    expect(pipe).toBeTruthy();
  });
});
