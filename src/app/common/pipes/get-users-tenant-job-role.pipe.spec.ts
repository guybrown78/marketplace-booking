import { GetUsersTenantJobRolePipe } from './get-users-tenant-job-role.pipe';

describe('GetUsersTenantJobRolePipe', () => {
  it('create an instance', () => {
    const pipe = new GetUsersTenantJobRolePipe();
    expect(pipe).toBeTruthy();
  });
});
