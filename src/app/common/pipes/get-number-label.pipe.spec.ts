import { GetNumberLabelPipe } from './get-number-label.pipe';

describe('GetNumberLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new GetNumberLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
