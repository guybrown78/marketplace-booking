import { GetLocationsPipe } from './get-locations.pipe';

describe('GetLocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
